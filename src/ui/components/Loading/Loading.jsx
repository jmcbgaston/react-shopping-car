import "./Loading.css";

// TODO: Replace with skeleton and shimmer
// Suspense + Boundary
export default function Loading() {
  return (
    <div className="loading">
      <p>Loading...</p>
    </div>
  );
}
